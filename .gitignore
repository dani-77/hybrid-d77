/build/
/apk-cache/
vendor/chimera-live/build/
vendor/chimera-live/apk-cache/
*.iso
*.iso.sha256
# cbuild build output (real h77-dots/h77-sway-dots .apk files) + the
# throwaway per-session signing key merged in iso/mklive-d77.sh -- both
# regenerated by container/cbuild.Containerfile, not meant to be
# tracked (the signing key changes every bootstrap).
/cbuild-out/
/iso/.keys/
__pycache__/
